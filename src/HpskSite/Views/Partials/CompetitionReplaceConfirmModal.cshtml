<!-- Replace Registration Confirmation Modal -->
<div class="modal fade" id="replaceRegistrationConfirmModal" tabindex="-1" aria-labelledby="replaceRegistrationConfirmModalLabel" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header bg-danger text-white">
				<h5 class="modal-title" id="replaceRegistrationConfirmModalLabel">
					<i class="bi bi-exclamation-triangle-fill"></i> Ersätt anmälan?
				</h5>
				<button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<div class="modal-body">
				<p id="replaceRegistrationMessage">
					Du är redan anmäld i <strong id="replaceExistingClassName"></strong>.
					Vill du ersätta den med <strong id="replaceNewClassName"></strong>?
				</p>
				<div class="alert alert-warning mb-0">
					<small>
						<i class="bi bi-info-circle"></i>
						<strong>OBS!</strong> Din anmälan i <strong id="replaceOldClassNameAlert"></strong> kommer att tas bort
						och ersättas med en ny anmälan i <strong id="replaceNewClassNameAlert"></strong>.
					</small>
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-bs-dismiss="modal" id="cancelReplaceBtn">
					<i class="bi bi-x-circle"></i> Avbryt
				</button>
				<button type="button" class="btn btn-danger" id="confirmReplaceBtn">
					<i class="bi bi-arrow-left-right"></i> Ersätt
				</button>
			</div>
		</div>
	</div>
</div>
