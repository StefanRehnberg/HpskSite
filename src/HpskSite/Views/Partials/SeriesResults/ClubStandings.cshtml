@*
 * Club Standings Partial
 * Renders one Club result section for a series.
 *
 * Note: Club standings are rendered client-side by the renderClubSection() function
 * in CompetitionSeries.cshtml. This partial exists as an anchor point and for
 * potential future server-side rendering needs.
 *
 * The renderClubSection function handles:
 * - Placement scoring (points different from raw score, shown with tooltip)
 * - BestOf non-counting cells (dimmed with strikethrough)
 * - Sum scoring (raw scores displayed directly)
 * - Grouped by class or combined ("Kombinerat")
*@

<div class="series-club-standings">
    <!-- Rendered by JavaScript in CompetitionSeries.cshtml -->
</div>
